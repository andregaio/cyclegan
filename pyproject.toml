[project]
name = 'cyclegan'
version = '0.1.2'
dynamic = ["dependencies", "optional-dependencies"]

[tool.setuptools.dynamic]
dependencies = { file = ["requirements.txt"] }

[tool.setuptools]
py-modules = ['cyclegan']
